<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bienvenido</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .container {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            width: 100%;
            max-width: 600px;
            padding: 40px;
            animation: slideIn .6s ease-out;
        }

        h2 {
            color: #333;
            text-align: center;
            margin-bottom: 10px;
            font-size: 30px;
        }

        .section {
            margin-top: 30px;
            padding: 20px;
            background: #f7f7f7;
            border-radius: 15px;
            box-shadow: inset 0 0 10px rgba(0,0,0,.05);
        }

        .label {
            font-weight: bold;
            color: #555;
        }

        .value {
            margin-bottom: 10px;
        }

        .btn {
            display: block;
            text-align: center;
            padding: 12px 30px;
            margin-top: 20px;
            background: #667eea;
            color: #fff;
            text-decoration: none;
            font-weight: bold;
            border-radius: 10px;
        }

        .btn:hover {
            background: #5a6fe0;
        }
    </style>
</head>
<body>

<div class="container">

    <h2>¡Bienvenido <%= user %>! 🎉</h2>
    <p style="text-align:center; color:#666;">Has iniciado sesión exitosamente</p>

    <div class="section">
        <h3>Tu Hoja de Vida</h3>

        <% if (profile) { %>
            <p><span class="label">Nombre completo:</span> <span class="value"><%= profile.full_name %></span></p>
            <p><span class="label">Fecha de nacimiento:</span> <span class="value"><%= profile.birthdate %></span></p>
            <p><span class="label">Dirección:</span> <span class="value"><%= profile.address %></span></p>
            <p><span class="label">Teléfono:</span> <span class="value"><%= profile.phone %></span></p>
            <p><span class="label">Biografía:</span> <span class="value"><%= profile.bio %></span></p>
        <% } else { %>
            <p style="color:#999;">Aún no tienes un perfil creado.</p>
        <% } %>

        <a class="btn" href="/profile">Editar Hoja de Vida</a>
        <a class="btn" href="/logout" style="background:#764ba2">Cerrar sesión</a>
    </div>

</div>

</body>
</html>
